<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10 text-white">
            <form action="/create" method="post">
                <input class="block px-5 rounded-md w-full py-2 outline-none bg-zinc-800" type="text" placeholder="Title" name="title"/>
                <textarea class="block rounded-md w-full px-5 mt-3 py-2 outline-none resize-none bg-zinc-800" placeholder="Task description..." name="description"></textarea>
                

                <select 
                    name="fileextension" 
                    id="fileextension" 
                    class="block rounded-md w-full px-5 mt-3 py-2 outline-none resize-none bg-zinc-800"
                >
                    <option value="txt">txt</option>
                    <option value="pdf">pdf</option>
                    <option value="docx">docx</option>
                    <option value="pptx">pptx</option>
                </select>
                <input class="mt-3 bg-blue-700 px-5 py-2 rounded-lg" type="submit" value="Create Task">
            </form>
        </div>
        <div class="tasks p-10 flex gap-3 flex-wrap">
            <% if(files.length>0){ %>
                <% files.forEach((file) => { %>
                    <div class="task min-w-72 px-3 py-4 bg-zinc-800 rounded-md outline-none">
                        
                        <div class="flex w-full justify-between items-center">
                            <h1 class="text-white text-3xl tracking-tighter"><%= file %></h1>
                            <a class="inline-block" href="/edit/<%= file %>">
                                <button class="bg-transparent px-3 py-2 text-white mt-1 rounded-lg">✏️</button>
                            </a>
                        </div>
                        <a class="inline-block" href="/files/<%= file %>">
                            <button class="bg-orange-600 px-3 py-2 text-white mt-3 rounded-lg">Read More</button>
                        </a>
                    </div>
                <% }) %>
            <% } else{ %>
                <h3 class="text-zinc-500">No task yet..</h3>
            <% } %>
            
            
            
            
        </div>
    </div>
    
</body>
</html>